trigger ThebingWebhookContactWebhookTrigger on Contact (after insert,after update,after delete,after undelete) {

    String url = 'https://tamwood.thebing.com/wdmvc/ts-salesforce/webhook/contact';

    String content = Webhook.jsonContent(Trigger.new, Trigger.old);

    Webhook.callout(url, content);

}